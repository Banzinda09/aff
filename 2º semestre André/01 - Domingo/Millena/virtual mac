# python3 -m venv (nome que quiser)

# para ativar no terminal do mac e so digitar o comando abaixo

# source (nome do venv)/bin/activate

#para intalar e so escrever com pip3

# para desativar e so digitar deactivate

testar se python3 e pip3 e a mesma coisas que o pip e python 